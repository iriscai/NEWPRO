CREATE TABLE
	`dwi_eqp_mc_line_list` (
		`id` string COMMENT '系统索引值',
		`line_no` string COMMENT '产线编号',
		`line_name` string COMMENT '产线名称',
		`factory_id` string COMMENT '生产厂',
		`resp_user_id` string COMMENT '产线负责人',
		`safe_resp_user` string COMMENT '安全负责人',
		`major_resp_user` string COMMENT '专业技术负责人',
		`line_type` string COMMENT '产线类别',
		`region_id` string COMMENT '区域',
		`line_status` string COMMENT '产线状态',
		`last_time` string COMMENT '上次状态发生时间',
		`access_overwrite` string COMMENT '是否覆盖推送',
		`access_plan_judge` string COMMENT '是否推送计划类记录',
		`access_statistic` string COMMENT '是否考核统计',
		`access_is_plan` string COMMENT '计划停机类型',
		`line_name_abbreviation` string COMMENT '产线名称简称',
		`remark` string COMMENT '备注',
		`create_user_no` string COMMENT '创建人',
		`create_date_time` string COMMENT '创建时间',
		`update_user_no` string COMMENT '修改人',
		`update_date_time` string COMMENT '修改时间',
		`company_id` string COMMENT '公司别',
		`status` decimal(1, 0) COMMENT '状态',
		`i_level` string COMMENT '排序',
		`is_delay` string COMMENT '是否延误：0停机,1延误',
		`ext_etl_dt` timestamp COMMENT 'ETL时间',
		`ext_src_sys_id` string COMMENT '源系统'
	) COMMENT '存放各厂的产线基本信息，包含产线名称、产线类别（主要、重要、辅助）、产线实时状态、是否纳入考核统计等；数据源于期初用户配置的产线信息，产线状态根据延误开停机信号实时变更，数据用于记录产线实时信息。' ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.orc.OrcSerde' STORED AS INPUTFORMAT 'org.apache.hadoop.hive.ql.io.orc.OrcInputFormat' OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.orc.OrcOutputFormat' LOCATION 'hdfs://hacluster/user/hive/warehouse/dwi_eqp.db/dwi_eqp_mc_line_list' TBLPROPERTIES (
		'bucketing_version' = '2',
		'orc.compress' = 'ZLIB',
		'transient_lastDdlTime' = '1710137200'
	)