CREATE TABLE
	`dwi_prd_tbid02` (
		`compid` string COMMENT '公司别',
		`id` string COMMENT '捆(卷)号',
		`costcenter` string COMMENT '成本中心',
		`management` string COMMENT '管理单位',
		`salesorg` string COMMENT '销售组别',
		`ipcode` string COMMENT '产品编码',
		`prodtypeno` string COMMENT '产品形态代码',
		`prodclassno` string COMMENT '产品大类',
		`prodtype` string COMMENT '成品类型',
		`tradeno` string COMMENT '钢种/牌号',
		`prodlen` double COMMENT '长度',
		`prodwgt` double COMMENT '实际重量',
		`theoryprodwgt` double COMMENT '理论重量',
		`bundleqty` double COMMENT '数量',
		`prodcaliber` double COMMENT '直径',
		`prodthick` double COMMENT '厚度',
		`prodwidth` double COMMENT '宽度',
		`inspodisp` string COMMENT '外观判定代码',
		`inspidisp` string COMMENT '试验判定代码',
		`prodstus` string COMMENT '状态',
		`preparestus` string COMMENT '存货准备状态',
		`returned` string COMMENT '是否退货',
		`heatno` string COMMENT '炉号',
		`storeid` string COMMENT '库代码',
		`storesort` string COMMENT '库分类码',
		`acrossid` string COMMENT '跨',
		`areaid` string COMMENT '区代码',
		`locx` string COMMENT '排',
		`layerid` string COMMENT '层',
		`locy` string COMMENT '列',
		`instockdate` string COMMENT '入库日期',
		`instockemp` string COMMENT '入库人员',
		`instockno` string COMMENT '入库单号',
		`orderno` string COMMENT '订单编号',
		`orderitem` string COMMENT '订单项次',
		`custno` string COMMENT '客户编号',
		`usertype` string COMMENT '用途码',
		`spflda` string COMMENT '存货特殊要求',
		`spfldb` string COMMENT '存货特殊要求',
		`spfldc` string COMMENT '存货特殊要求',
		`spfldd` string COMMENT '存货特殊要求',
		`mscno` string COMMENT 'MSC-NO',
		`msclineno` string COMMENT 'MSC-Line-No',
		`lotno` string COMMENT '轧制序号',
		`outputshift` string COMMENT '产出班别',
		`proddate` string COMMENT '生产日期',
		`prodtime` string COMMENT '生产时间',
		`judreasonno` string COMMENT '改判原因',
		`juddate` string COMMENT '改判日期',
		`dislistno` string COMMENT '发货通知单',
		`salereturnno` string COMMENT '退货单号',
		`loadtailing` string COMMENT '装车明细表',
		`shiporderno` string COMMENT '出货明细表',
		`shippingdate` string COMMENT '出货日期',
		`releasedate` string COMMENT '放行日期',
		`raveingstus` string COMMENT '拆捆状态',
		`raveingpackid` string COMMENT '拆捆前捆号',
		`isonepass` string COMMENT '一次判过',
		`prodline` string COMMENT '产线别',
		`sourcetype` string COMMENT '来源',
		`isspare` string COMMENT '是否为余材',
		`isowner` string COMMENT '是否为有主库存',
		`loadcarbatch` string COMMENT '车号',
		`isleave` string COMMENT '是否为寄仓',
		`grade` string COMMENT '品级',
		`isonway` string COMMENT '是否为在途',
		`packingmode` string COMMENT '包装方式',
		`leavedate` string COMMENT '寄仓日期',
		`shippingemp` string COMMENT '出货销帐人员',
		`lshippingemp` string COMMENT '寄仓出货人员',
		`lshippingdate` string COMMENT '寄仓出货日期',
		`createempno` string COMMENT '建立人员',
		`createdate` string COMMENT '建立日期',
		`updateempno` string COMMENT '修改人员',
		`updatedate` string COMMENT '修改日期',
		`updatetime` string COMMENT '修改时间',
		`finalprogram` string COMMENT '最后的维护程式',
		`memo` string COMMENT '备注',
		`arstatus` string COMMENT '起始状态',
		`artimestamp` string COMMENT '起始时间戳',
		`disreason` string COMMENT '处置原因',
		`dispro` string COMMENT '处置方案',
		`realprodwgt` double COMMENT '实际重量',
		`stackid` string COMMENT '垛',
		`psrno` string COMMENT '产品规范代码',
		`allotorderno` string COMMENT '分配订单编号',
		`allotorderitem` string COMMENT '分配订单项次',
		`holdstus` string COMMENT '暂留状态',
		`samplingpdirecno` string COMMENT '流程指示序号',
		`ishand` string COMMENT '是否手持',
		`stdclasschin` string COMMENT '标准类别名称',
		`amisscode` string COMMENT '缺陷代码',
		`respdept` string COMMENT '责任单位',
		`specmark` string COMMENT '规格描述',
		`ext_etl_dt` string,
		`ext_src_sys_id` string
	) COMMENT '进行特钢轧制序号、捆号、订单库存的记录，围绕钢材的产出、判定、储位进行库存的变化记录详情，包括捆号、生产时间、重量、状态、入库时间等信息' ROW FORMAT SERDE 'org.apache.hadoop.hive.serde2.columnar.ColumnarSerDe' STORED AS INPUTFORMAT 'org.apache.hadoop.hive.ql.io.RCFileInputFormat' OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.RCFileOutputFormat' LOCATION 'hdfs://hacluster/user/hive/warehouse/dwi_prd.db/dwi_prd_tbid02' TBLPROPERTIES (
		'bucketing_version' = '2',
		'transient_lastDdlTime' = '1756073230'
	)