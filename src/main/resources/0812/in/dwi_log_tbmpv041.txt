CREATE TABLE
	`dwi_log_tbmpv041` (
		`compid` string COMMENT '公司代码',
		`inspno` string COMMENT '验收单号(结算单号)',
		`pono` string COMMENT '订购单号',
		`poitemno` string COMMENT '订购项次',
		`purno` string COMMENT '采购案号',
		`puritemno` string COMMENT '采购案项次',
		`matrlno` string COMMENT '物料编号',
		`recvqty` double COMMENT '收货数量',
		`inspqty` double COMMENT '验收数量',
		`qualiqty` double COMMENT '合格量',
		`returnqty` double COMMENT '退货数量',
		`freeqty` double COMMENT '原燃料结算管理费计算之前的贷款总金额备份',
		`isbs` string COMMENT '是否需要二维码',
		`rejectqty` double COMMENT '让步接收量',
		`saveinvqty` double COMMENT '入库数量',
		`inspuintprice` double COMMENT '存货成本单价',
		`wgtamt` double COMMENT '货款金额',
		`modamt` double COMMENT '计量分摊(调整金额）',
		`inspamt` double COMMENT '验收金额',
		`pobatchamt` double COMMENT '订购批次金额',
		`othertranscostamt` double COMMENT '第三方运杂费(未税)',
		`transcostamt` double COMMENT '厂商待垫运杂费',
		`transcostnotaxamt` double COMMENT '厂商待垫运杂费(不含税)',
		`taxrate` double COMMENT '营业税率',
		`beforetaxamt` double COMMENT '税前金额',
		`taxamt` double COMMENT '营业税',
		`totalamt` double COMMENT '税后金额',
		`invamt` double COMMENT '货物成本金额',
		`invaaamt` double COMMENT '验收记帐币别金额',
		`stogcode` string COMMENT '储位代号',
		`lotno` string COMMENT '批号',
		`delaydays` double COMMENT '逾期天数',
		`isdirissue` string COMMENT '是否直接发料或分存',
		`shipno` string COMMENT '船運編號',
		`memo` string COMMENT '备注',
		`costcenter` string COMMENT '成本中心',
		`projno` string COMMENT '工程编号',
		`isprintqrcode` string COMMENT '是否需要打印标识码',
		`vendorqty` double COMMENT '卖方矿发数',
		`vendorweighednum` double COMMENT '卖方检金数',
		`settlenum` double COMMENT '我方结算重量',
		`weighednum` double COMMENT '我方检金数',
		`wgtlistno` string COMMENT '过磅单号(费用申请单)',
		`rangeamt` double COMMENT '涨幅金额(基地废钢)/卖方结算量折干量(原燃料)',
		`adddeamt` double COMMENT '追加扣款金额',
		`totaldeamt` double COMMENT '自动扣款金额',
		`ishonest` string COMMENT '是否诚信(Y,N)',
		`strmemoa` string COMMENT '预留1',
		`strmemob` string COMMENT '预留2',
		`strmemoc` string COMMENT '预留3',
		`crtemplno` string COMMENT '资料建立人员',
		`crtdate` string COMMENT '资料建立日期',
		`crttime` string COMMENT '资料建立时间',
		`updemplno` string COMMENT '资料异动人员',
		`upddate` string COMMENT '资料异动日期',
		`updtime` string COMMENT '资料异动时间',
		`isautomatic` string COMMENT '是否自动入库',
		`rebatedeductamt` double COMMENT '（废钢）返利扣减金额',
		`isrebatededuct` string COMMENT '（废钢）是否返利扣减(Y,N)',
		`adduintprice` double COMMENT '单价阶梯奖励金额',
		`ischeck` string COMMENT '是否抽检',
		`checkstatus` string COMMENT '抽检状态',
		`checkopinion` string COMMENT '抽检意见',
		`compidold` string COMMENT '原公司别',
		`ext_etl_dt` timestamp COMMENT 'ETL时间',
		`ext_src_sys_id` string COMMENT '源系统'
	) COMMENT '录入备件辅材采购入库明细档数据，对象是针对已经验收合格物料，由仓库进行入库操作，内容包含料号，数量，储位，批号等' ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.orc.OrcSerde' STORED AS INPUTFORMAT 'org.apache.hadoop.hive.ql.io.orc.OrcInputFormat' OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.orc.OrcOutputFormat' LOCATION 'hdfs://hacluster/user/hive/warehouse/dwi_log.db/dwi_log_tbmpv041' TBLPROPERTIES (
		'bucketing_version' = '2',
		'orc.compress' = 'ZLIB',
		'transient_lastDdlTime' = '1706144549'
	)