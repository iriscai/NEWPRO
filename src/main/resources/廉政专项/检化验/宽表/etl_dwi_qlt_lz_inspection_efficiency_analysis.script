{
	"configuration":{},
	"connectionName":"MRS_Spark_Agent",
	"content":"-- SPARK sql \n-- ******************************************************************** --\n-- author: jh_caiyi\n-- create time: 2025-07-03\n-- descr: 廉政八项-检化验专线-检化验时间效率分析宽表(含复样和合同结算数据)\n-- ******************************************************************** --\n-- DROP TABLE IF EXISTS dwi_qlt.dwi_qlt_lz_inspection_base_detail;\n-- CREATE TABLE\n-- \tdwi_qlt.dwi_qlt_lz_inspection_base_detail (\n--     smp_no STRING COMMENT '样品编号',\n--     heat_no STRING COMMENT '炉次号',\n--     smp_type STRING COMMENT '试样类型',\n--     checkno STRING COMMENT '检验批号',\n--     sub_mat_no STRING COMMENT '物料类别代码',\n--     mtrl_no STRING COMMENT '物料牌号代码',\n--     use_dept_no STRING COMMENT '使用单位代码',\n    \n--     -- 取样环节\n--     get_smp_day STRING COMMENT '取样日期',\n--     get_smp_time STRING COMMENT '取样时间',\n--     smp_date_start STRING COMMENT '取样开始日期',\n--     smp_time_start STRING COMMENT '取样开始时间',\n--     smp_date_time_end STRING COMMENT '取样结束时间',\n--     dateduration STRING COMMENT '取样周期时长',\n--     get_smp STRING COMMENT '取样人',\n--     get_smp_status STRING COMMENT '取样状态',\n    \n--     -- 送样环节\n--     smp_send_day STRING COMMENT '送样日期',\n--     smp_send_time STRING COMMENT '送样时间',\n--     send_day STRING COMMENT '发送日期(检测站送检验日期)',\n--     send_time STRING COMMENT '发送时间(检测站送检验时间)',\n--     smp_sender STRING COMMENT '送样人',\n    \n--     -- 制样环节\n--     smp_mk_date STRING COMMENT '制样日期(破碎缩分)',\n--     smp_mk_time STRING COMMENT '制样时间(破碎缩分)',\n--     smp_mk_date_ym STRING COMMENT '研磨制样日期(研磨)',\n--     smp_mk_time_ym STRING COMMENT '研磨制样时间(研磨)',\n--     mk_rcv_date STRING COMMENT '制样接收日期',\n--     mk_rcv_time STRING COMMENT '制样接收时间',\n--     mk_upd_date STRING COMMENT '制样确认日期',\n--     mk_upd_time STRING COMMENT '制样确认时间',\n--     smp_maker STRING COMMENT '制样人(破碎缩分)',\n--     smp_maker_ym STRING COMMENT '研磨制样人(研磨)',\n--     mk_upd_emp STRING COMMENT '制样确认人',\n    \n--     -- 检验环节\n--     receive_day STRING COMMENT '样品接收日期(化验站接收样品日期)',\n--     receive_time STRING COMMENT '样品接收时间(化验站接收样品时间)',\n--     smp_item STRING COMMENT '检验item',\n--     smp_date_time STRING COMMENT '检验时间',\n    \n--     -- 审核环节\n--     judg_day STRING COMMENT '审核日期',\n--     judg_time STRING COMMENT '审核时间',\n--     judg_day3 STRING COMMENT '检测站审核日期',\n--     judg_time3 STRING COMMENT '检测站审核时间',\n--     confirm_date STRING COMMENT '数据确认日期',\n--     confirm_time STRING COMMENT '数据确认时间',\n--     judgeer STRING COMMENT '审核人',\n--     judgeer3 STRING COMMENT '检测站审核人',\n    \n--     -- 复样相关信息\n--     retest_flag STRING COMMENT '复样标识',\n--     smp_mk_code_old STRING COMMENT '原样品编号',\n--     compId STRING COMMENT '公司代码',\n--     inspNo STRING COMMENT '结算单号',\n--     chkNo STRING COMMENT '检验批号',\n--     mtrlNo STRING COMMENT '料号',\n--     pover STRING COMMENT '合同版本号',\n--     totalDeDuctAmt STRING COMMENT '加扣款金额',\n--     miNo STRING COMMENT '入库单号',\n--     transType STRING COMMENT '结算类别',\n--     transMode STRING COMMENT '结算型态',\n--     exchangeRate STRING COMMENT '汇率',\n--     taxRate STRING COMMENT '发票税率',\n--     taxAmt STRING COMMENT '发票税额合计',\n--     transCostAmt STRING COMMENT '暂估第三方运费合计',\n--     totalAmt STRING COMMENT '验收金额合计',\n--     totalQty STRING COMMENT '装载重量合计',\n--     invoiceQty STRING COMMENT '发票重量',\n--     invoiceAmt STRING COMMENT '发票金额',\n--     onRoadAmt STRING COMMENT '冲销在途金额',\n--     isFinally STRING COMMENT '结算类型',\n    \n--     -- 其他信息\n--     urgent_flag STRING COMMENT '加急标记',\n--     experiment STRING COMMENT '实验',\n--     join_virsmp STRING COMMENT '参与虚拟样生成',\n--     makecheck_flag STRING COMMENT '检验标记',\n--     info_source STRING COMMENT '信息来源',\n--     flag STRING COMMENT '完成标志',\n--     inspempl_no STRING COMMENT '检验站编号'\n--     \t) COMMENT '检化验基础数据明细表(含复样和合同结算数据)' STORED AS orc TBLPROPERTIES (\"orc.compress\" = \"SNAPPY\");\n-- 数据插入语句\n-- 基础数据明细表数据插入\nDROP VIEW IF EXISTS  dwi_qlt.dwi_qlt_lz_inspection_sampinfo_view;\nCREATE VIEW dwi_qlt.dwi_qlt_lz_inspection_sampinfo_view AS \nSELECT * FROM dwi_qlt.dwi_qlt_tx_sampinfo_lz where ins_date>'20250601';\n-- SELECT * FROM  dwi_qlt.dwi_qlt_lz_inspection_sampinfo_view WHERE smp_no='Y01100Y1SP739842';\n-- 取样动作时长\nDROP VIEW IF EXISTS  dwi_qlt.dwi_qlt_lz_inspection_smp_rec_duration_view;\n-- SELECT * FROM  dwi_qlt.dwi_qlt_lz_inspection_smp_rec_duration_view WHERE checkno='S2505C000701';\nDROP VIEW IF EXISTS  dwi_qlt.dwi_qlt_lz_inspection_smp_rec_view;\nCREATE VIEW dwi_qlt.dwi_qlt_lz_inspection_smp_rec_view AS \nSELECT *,from_unixtime(unix_timestamp(concat(smp_date_start, smp_time_start), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') AS smp_date_time_start,from_unixtime(unix_timestamp(concat(smp_date_end, smp_time_end), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') AS smp_date_time_end FROM dwi_qlt.dwi_qlt_tx_smp_rec_lz ;\nCREATE VIEW dwi_qlt.dwi_qlt_lz_inspection_smp_rec_duration_view AS \nSELECT\n\tdiff_smp_rec.checkno,\n\tdiff_smp_rec.smp_type,\n\tdiff_smp_rec.smp_date_time_start,\n\tdiff_smp_rec.smp_date_time_end,\n\tCASE\n\t\tWHEN diff_smp_rec.smp_date_time_end IS NULL THEN '999999' \n\t\tELSE (unix_timestamp(diff_smp_rec.smp_date_time_end, 'yyyy-MM-dd HH:mm:ss') - unix_timestamp(diff_smp_rec.smp_date_time_start, 'yyyy-MM-dd HH:mm:ss'))\n\tEND AS dateduration\nFROM\n\t(\n\t\tSELECT\n\t\t\tcheckno,\n\t\t\tsmp_type,\n\t\t\tMIN(smp_date_time_start) AS smp_date_time_start,\n\t\t\tMAX(smp_date_time_end) AS smp_date_time_end\n\t\t\t-- 计算时间差（以分钟为单位）\n\t\t\t-- ROUND((unix_timestamp(end_time, 'yyyy-MM-dd HH:mm:ss') - unix_timestamp(start_time, 'yyyy-MM-dd HH:mm:ss')) / 60, 2) AS diff_minutes,\n\t\tFROM\n\t\t\tdwi_qlt.dwi_qlt_lz_inspection_smp_rec_view\n\t\tGROUP BY\n\t\t\tcheckno,\n\t\t\tsmp_type\n\t) diff_smp_rec;\n-- where SMP_DATE_START>'20250601';\n-- 检验环节\n-- SELECT * FROM  dwi_qlt.dwi_qlt_lz_inspection_analyresult_duration_view WHERE smp_no='Y01100Y1SP739842' AND sam_checkno='';\nDROP VIEW IF EXISTS  dwi_qlt.dwi_qlt_lz_inspection_analyresult_duration_view;\nDROP VIEW IF EXISTS  dwi_qlt.dwi_qlt_lz_inspection_analyresult_view;\nCREATE VIEW dwi_qlt.dwi_qlt_lz_inspection_analyresult_view AS \nSELECT *,from_unixtime(unix_timestamp(concat(smp_date, smp_time), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') AS smp_date_time FROM dwi_qlt.dwi_qlt_tx_analyresult_lz where SMP_DATE>'20250601';\nCREATE VIEW dwi_qlt.dwi_qlt_lz_inspection_analyresult_duration_view AS \nSELECT smp_no, heat_no, smp_item,smp_type,sam_checkno,\n-- tester,test_eqt_no,\nMAX(smp_date_time) AS smp_date_time FROM dwi_qlt.dwi_qlt_lz_inspection_analyresult_view \n     GROUP BY smp_no, heat_no, smp_item,smp_type,sam_checkno\n    --  tester,test_eqt_no\n     ;\n-- 复样环节\n-- SELECT * FROM dwi_qlt.dwi_qlt_tx_mr_code_manage_lz where ins_date>'20250601';\nDROP VIEW IF EXISTS  dwi_qlt.dwi_qlt_lz_inspection_code_manage_view;\nCREATE VIEW dwi_qlt.dwi_qlt_lz_inspection_code_manage_view AS    \nSELECT smp_no,smp_type,checkno,smp_mk_code_old,smp_mk_code,smp_test_code FROM dwi_qlt.dwi_qlt_tx_mr_code_manage_lz WHERE smp_mk_code_old is not null;\n\n-- 财务结算\n-- SELECT compId,inspNo,chkNo,mtrlNo,pover,deDuctAmtStr,deDuctAmt,totalDeDuctAmt,unitPrice,totalAmt,weighedNum,modNum,settleNum FROM dwi_qlt.dwi_qlt_tbmpv046_lz;\nDROP VIEW IF EXISTS  dwi_qlt.dwi_qlt_lz_inspection_tbmpv046_view;\nCREATE VIEW dwi_qlt.dwi_qlt_lz_inspection_tbmpv046_view AS \nSELECT compId,inspNo,chkNo,mtrlNo,pover,sum(totalDeDuctAmt) AS totalDeDuctAmt FROM dwi_qlt.dwi_qlt_tbmpv046_lz group by compId,inspNo,chkNo,mtrlNo,pover;\n\nINSERT OVERWRITE TABLE dwi_qlt.dwi_qlt_lz_inspection_base_detail \n-- PARTITION(dt='${bdp.system.bizdate}')\nSELECT\n    a.smp_no,                                                       -- 样品编号\n    a.heat_no,                                                      -- 炉次号\n    a.smp_type,                                                     -- 试样类型\n    a.checkno,                                                      -- 检验批号\n    a.sub_mat_no,                                                   -- 物料类别代码\n    a.mtrl_no,                                                      -- 物料牌号代码\n    a.use_dept_no,                                                  -- 使用单位代码\n    \n    -- 取样环节\n    a.get_smp_day,                                                  -- 取样日期\n    a.get_smp_time,                                                 -- 取样时间\n--     CASE\n-- \t\tWHEN a.get_smp_day IS NULL THEN NULL\n-- \t\tWHEN a.get_smp_time IS NULL THEN from_unixtime(unix_timestamp(concat(a.get_smp_day, '000000'), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss')\n-- \t\tELSE \n--     from_unixtime(unix_timestamp(concat(a.get_smp_day, a.get_smp_time), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') END AS get_smp_day_time,  --取样时间\n    a.smp_date_start,                                               -- 取样开始日期\n    a.smp_time_start,                                               -- 取样开始时间\n--     CASE\n-- \t\tWHEN a.smp_date_start IS NULL THEN NULL\n-- \t\tWHEN a.smp_time_start IS NULL THEN from_unixtime(unix_timestamp(concat(a.smp_date_start, '000000'), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss')\n-- \t\tELSE \n--     from_unixtime(unix_timestamp(concat(a.smp_date_start, a.smp_time_start), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') END AS smp_date_time_start,  --取样开始时间\n    -- c.smp_date_end,                                                 -- 取样结束日期\n    c.smp_date_time_end,                                                 -- 取样结束时间\n    c.dateduration,--取样周期时长\n    a.get_smp,                                                      -- 取样人\n    a.get_smp_status,                                               -- 取样状态\n    \n    -- 送样环节\n    a.smp_send_day,                                                 -- 送样日期\n    a.smp_send_time,                                                -- 送样时间\n--     CASE\n-- \t\tWHEN a.smp_send_day IS NULL THEN NULL\n-- \t\tWHEN a.smp_send_time IS NULL THEN from_unixtime(unix_timestamp(concat(a.smp_send_day, '000000'), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss')\n-- \t\tELSE \n--     from_unixtime(unix_timestamp(concat(a.smp_send_day, a.smp_send_time), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') END AS smp_send_date_time,  --送样时间\n    a.send_day,                                                     -- 发送日期(检测站送检验日期)\n    a.send_time,                                                    -- 发送时间(检测站送检验时间)\n--     CASE\n-- \t\tWHEN a.send_day IS NULL THEN NULL\n-- \t\tWHEN a.send_time IS NULL THEN from_unixtime(unix_timestamp(concat(a.send_day, '000000'), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss')\n-- \t\tELSE \n--     from_unixtime(unix_timestamp(concat(a.send_day, a.send_time), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') END AS send_date_time,  --发送时间(检测站送检验时间)\n    a.smp_sender,                                                   -- 送样人\n    \n    -- 制样环节\n    a.smp_mk_date,                                                  -- 制样日期(破碎缩分)\n    a.smp_mk_time,                                                  -- 制样时间(破碎缩分)\n--      CASE\n-- \t\tWHEN a.smp_mk_date IS NULL THEN NULL\n-- \t\tWHEN a.smp_mk_time IS NULL THEN from_unixtime(unix_timestamp(concat(a.smp_mk_date, '000000'), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss')\n-- \t\tELSE \n--     from_unixtime(unix_timestamp(concat(a.smp_mk_date, a.smp_mk_time), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') END AS smp_mk_date_time,  --制样时间(破碎缩分)\n    a.smp_mk_date_ym,                                               -- 研磨制样日期(研磨)\n    a.smp_mk_time_ym,                                               -- 研磨制样时间(研磨)\n--      CASE\n-- \t\tWHEN a.smp_mk_date_ym IS NULL THEN NULL\n-- \t\tWHEN a.smp_mk_time_ym IS NULL THEN from_unixtime(unix_timestamp(concat(a.smp_mk_date_ym, '000000'), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss')\n-- \t\tELSE \n--     from_unixtime(unix_timestamp(concat(a.smp_mk_date_ym, a.smp_mk_time_ym), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') END AS smp_mk_ym_date_time,  --研磨制样时间(研磨)\n    a.mk_rcv_date,                                                  -- 制样接收日期\n    a.mk_rcv_time,                                                  -- 制样接收时间\n--      CASE\n-- \t\tWHEN a.mk_rcv_date IS NULL THEN NULL\n-- \t\tWHEN a.mk_rcv_time IS NULL THEN from_unixtime(unix_timestamp(concat(a.mk_rcv_date, '000000'), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss')\n-- \t\tELSE \n--     from_unixtime(unix_timestamp(concat(a.mk_rcv_date, a.mk_rcv_time), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') END AS smp_mk_rcv_date_time,  --制样接收时间\n    a.mk_upd_date,                                                  -- 制样确认日期\n    a.mk_upd_time,                                                  -- 制样确认时间\n--      CASE\n-- \t\tWHEN a.mk_upd_date IS NULL THEN NULL\n-- \t\tWHEN a.mk_upd_time IS NULL THEN from_unixtime(unix_timestamp(concat(a.mk_upd_date, '000000'), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss')\n-- \t\tELSE \n--     from_unixtime(unix_timestamp(concat(a.mk_upd_date, a.mk_upd_time), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') END AS smp_mk_upd_date_time,  --制样确认时间\n    a.smp_maker,                                                    -- 制样人(破碎缩分)\n    a.smp_maker_ym,                                                 -- 研磨制样人(研磨)\n    a.mk_upd_emp,                                                   -- 制样确认人\n    \n    -- 检验环节\n    a.receive_day,                                                  -- 样品接收日期(化验站接收样品日期)\n    a.receive_time,                                                 -- 样品接收时间(化验站接收样品时间)\n--      CASE\n-- \t\tWHEN a.receive_day IS NULL THEN NULL\n-- \t\tWHEN a.receive_time IS NULL THEN from_unixtime(unix_timestamp(concat(a.receive_day, '000000'), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss')\n-- \t\tELSE \n--     from_unixtime(unix_timestamp(concat(a.receive_day, a.receive_time), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') END AS smp_receive_day_time,  --样品接收时间(化验站接收样品时间)\n    -- b.smp_date,                                                     -- 检验日期\n    -- b.smp_time,                                                     -- 检验时间\n    b.smp_item,                                                       -- 检验item\n      b.smp_date_time,                                                     -- 检验时间\n    -- b.tester,                                                       -- 检验员\n    -- b.test_eqt_no,                                                  -- 检验设备\n    \n    -- 审核环节\n    a.judg_day,                                                     -- 审核日期\n    a.judg_time,                                                    -- 审核时间\n--      CASE\n-- \t\tWHEN a.judg_day IS NULL THEN NULL\n-- \t\tWHEN a.judg_time IS NULL THEN from_unixtime(unix_timestamp(concat(a.judg_day, '000000'), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss')\n-- \t\tELSE \n--     from_unixtime(unix_timestamp(concat(a.judg_day, a.judg_time), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') END AS smp_judg_day_time,  --审核时间\n    a.judg_day3,                                                    -- 检测站审核日期\n    a.judg_time3,                                                   -- 检测站审核时间\n--      CASE\n-- \t\tWHEN a.judg_day3 IS NULL THEN NULL\n-- \t\tWHEN a.judg_time3 IS NULL THEN from_unixtime(unix_timestamp(concat(a.judg_day3, '000000'), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss')\n-- \t\tELSE \n--     from_unixtime(unix_timestamp(concat(a.judg_day3, a.judg_time3), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') END AS smp_judg_day3_time,  --检测站审核时间\n    a.confirm_date,                                                 -- 数据确认日期\n    a.confirm_time,                                                 -- 数据确认时间\n--      CASE\n-- \t\tWHEN a.confirm_date IS NULL THEN NULL\n-- \t\tWHEN a.confirm_time IS NULL THEN from_unixtime(unix_timestamp(concat(a.confirm_date, '000000'), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss')\n-- \t\tELSE \n--     from_unixtime(unix_timestamp(concat(a.confirm_date, a.confirm_time), 'yyyyMMddHHmmss'), 'yyyy-MM-dd HH:mm:ss') END AS smp_confirm_date_time,  --数据确认时间\n    a.judgeer,                                                      -- 审核人\n    a.judgeer3,                                                     -- 检测站审核人\n    \n    -- 复样相关信息\n    a.retest_flag,                                                  -- 复样标识\n    d.smp_mk_code_old,                                                 -- 原样品编号\n    e.compId,                                                  -- 公司代码\n    e.inspNo,                                                -- 结算单号\n    e.chkNo,                                                -- 检验批号\n    e.mtrlNo,                                                -- 料号\n    e.pover,                                                -- 合同版本号\n    e.totalDeDuctAmt,                                              -- 加扣款金额\n    f.miNo,                                            -- 入库单号\n    f.transType,                                            -- 结算类别\n    f.transMode,                                            -- 结算型态\n    f.exchangeRate,                                          --    汇率\n    f.taxRate,                                               --发票税率\n    f.taxAmt,                                                --发票税额合计\n    f.transCostAmt,                                          --    暂估第三方运费合计\n    f.totalAmt,                                              --验收金额合计\n    f.totalQty,                                              --装载重量合计\n    f.invoiceQty,                                            --    发票重量\n    f.invoiceAmt,                                            --    发票金额\n    f.onRoadAmt,                                             --    冲销在途金额\n    f.isFinally        ,                                     --            结算类型\n    -- e.settlement_amount,                                            -- 结算金额\n    -- e.settlement_date,                                              -- 结算日期\n    -- e.settlement_status,                                            -- 结算状态\n    -- e.quality_deduction,                                            -- 质量扣款\n    \n    -- 其他信息\n    a.urgent_flag,                                                  -- 加急标记\n    a.experiment,                                                   -- 实验\n    a.join_virsmp,                                                  -- 参与虚拟样生成\n    a.makecheck_flag,                                               -- 检验标记\n    a.info_source,                                                  -- 信息来源\n    a.flag,                                                         -- 完成标志\n    a.inspempl_no                                                    -- 检验站编号\nFROM \n    dwi_qlt.dwi_qlt_lz_inspection_sampinfo_view a\nLEFT JOIN \ndwi_qlt.dwi_qlt_lz_inspection_analyresult_duration_view b\nON a.smp_no = b.smp_no AND a.heat_no = b.heat_no AND a.smp_type = b.smp_type AND a.checkno = b.sam_checkno\nLEFT JOIN \ndwi_qlt.dwi_qlt_lz_inspection_smp_rec_duration_view  c\nON a.checkno = c.checkno AND a.smp_type = c.smp_type\n-- 关联复样数据\nLEFT JOIN \ndwi_qlt.dwi_qlt_lz_inspection_code_manage_view d\nON a.smp_no = d.smp_mk_code_old\n-- 关联结算扣款数据\nLEFT JOIN \n   dwi_qlt.dwi_qlt_lz_inspection_tbmpv046_view e\nON  a.checkno = e.chkNo\nLEFT JOIN \n   dwi_qlt.dwi_qlt_tbmpv040_lz f\nON  f.inspNo = e.inspNo\nWHERE a.smp_no IS NOT NULL;\n\n\n-- SELECT count(1) FROM dwi_qlt.dwi_qlt_lz_inspection_base_detail;",
	"currentDatabase":"dwi_qlt",
	"description":"",
	"directory":"/廉政专项/检化验/宽表",
	"name":"etl_dwi_qlt_lz_inspection_efficiency_analysis",
	"templateVersion":"1.0",
	"type":"SparkSQL"
}